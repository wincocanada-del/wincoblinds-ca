# Winco 홈페이지 관리 구조

2026-09-24: 설계안을 바탕으로 관리자 기능을 구현했습니다. 실제 운영 확인 결과는 WEBSITE-ADMIN-GUIDE.md에 기록합니다.

## 직원 화면

`https://wincoblinds.ca/admin/`에서 이메일 로그인 후 페이지, 메뉴·하위 메뉴, 갤러리 사진, 원단, 소식·글, 매장 정보를 편집합니다. 기존 디자인을 유지하는 내용 블록을 추가·이동·숨김 처리할 수 있습니다. 새로운 예약·결제 기능이나 디자인 자체의 변경은 별도 개발이 필요합니다.

소유자는 wincocanada@gmail.com입니다. 편집 직원은 초안 저장과 미리보기, 게시 담당자는 게시까지 가능합니다. 소유자만 직원 권한을 등록·해제합니다. 이메일을 등록하는 버튼은 초대 메일을 보내지 않습니다. 추가 직원의 로그인 메일 발송에는 Supabase의 이메일 발송 설정이 해당 주소를 지원해야 합니다.

## 저장 및 게시

- 초안과 변경 이력은 Supabase의 홈페이지 전용 `winco_web_*` 테이블에 보관합니다. 로그인과 권한을 데이터베이스에서 검사합니다.
- 저장 시 버전을 비교하므로 다른 직원의 변경을 조용히 덮어쓰지 않습니다.
- 게시 버튼은 저장된 버전을 확인하고 숨김 페이지·블록과 미사용 사진을 제외한 공개 스냅샷을 만듭니다. **게시 요청한 스냅샷은 배포 완료 전에도 공개 데이터로 취급됩니다.**
- Netlify Function이 게시 권한을 확인한 뒤 서버 전용 `WEBSITE_BUILD_HOOK`으로 main 빌드를 요청합니다. 이 값은 Netlify Production의 비밀 환경변수입니다. 브라우저나 저장소에 넣지 않습니다.
- Netlify는 공개 스냅샷을 읽고 HTML과 사진을 생성합니다. 사진도 해당 배포에 포함되므로 이전 배포로 되돌릴 때 함께 복원됩니다. 실패하면 기존 운영 배포가 유지됩니다.
- 관리자 화면은 실제 `/website-version.json`이 요청한 버전으로 바뀐 경우에만 게시 완료를 표시합니다. 6분 동안 확인하지 못하면 Netlify 상태를 확인하도록 안내합니다.
- 이전 내용으로 되돌리기는 변경 이력에서 불러와 새 초안으로 저장·게시합니다. 과거 기록과 업로드는 유지합니다.

## 사진과 보안

업로드 사진은 JPG/PNG/WebP 20MB 이하를 받아 긴 변 1920px 이하 WebP로 변환합니다. 저장 파일은 5MB 이하입니다. 고유 파일명과 비공개 Storage 버킷을 사용하고, 게시 버전에 포함된 사진만 방문자가 읽을 수 있습니다. 초안 미리보기는 만료되는 서명 주소를 사용합니다.

관리자와 빌드는 공개 anon 키를 사용합니다. 초안, 직원 목록, 버전 조회는 익명으로 접근할 수 없습니다. service_role 키는 필요하지 않습니다. 관리자 화면은 검색 색인에서 제외하며 임의 HTML·스크립트 입력은 허용하지 않습니다.

## 기존 사이트와 앱

고객 문의는 기존 Netlify Forms → Winco Gmail 알림을 유지합니다. 관리자에서 바꾸는 공개 이메일과 문의 수신 알림 주소는 별도입니다.

빌드는 Git에 커밋된 `app/` 파일을 그대로 복사하며 `/app/*` 연결 규칙을 유지합니다. 로컬에만 있는 앱 변경 파일을 홈페이지 배포에 섞지 않습니다.

## 개발 및 운영

- 초기 콘텐츠: `cms/seed.json`, 기존 디자인 블록: `cms/templates.json`
- 실제 첫 게시 이후 콘텐츠 원본: Supabase 초안 및 게시 버전
- 렌더러: `cms/render.mjs`, 관리자: `admin/`, 게시 함수: `netlify/functions/website-publish.mjs`
- 일회성 스키마: `cms/schema.sql` (이미 적용한 프로젝트에 재실행하지 않음)
- `npm ci`, `npm run build`, `npm run check`, `npm test`
- 로컬에서 공개 게시 데이터를 사용하려면 `CMS_REMOTE=1`; 일반 로컬 빌드는 초기 콘텐츠를 사용합니다.
- `npm run preview`: localhost:5501. `/admin/?demo=1`은 로컬 화면 확인용이며 실제 저장·게시를 하지 않습니다.

루트의 기존 HTML과 assets/data는 이전 정적 사이트 자료입니다. 현재 운영 콘텐츠 수정은 관리자에서 합니다. 기존 정적 생성 스크립트를 실행해도 게시된 CMS 내용이 바뀌지 않습니다.
